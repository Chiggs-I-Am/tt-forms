/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [ "https://picsum.photos", "picsum.photos", "lh3.googleusercontent.com" ],
  },
}

module.exports = nextConfig
